# 附件X — agy 复核报告：命名统一批次（书声 / ebook2tts）

> 版本 **v1.0** ｜ 2026-09-21 ｜ 复核方：**agy**（`gemini-3.8-flash-high` + `--effort high`，美国出口 `🇺🇸 Sakura美国原生 05 AI` / 77.111.110.75）
> 被复核对象：README 重构 + 仓库内引用同步（7 文件、22 处编辑）
> 复核方式：**单轮内联**（完整 `git diff HEAD` ＋ 自测结果内联，明示禁止其读文件/用工具）
> **复核结论：【通过】**（9 条防呆红线全部合规；附 1 条非阻塞运维建议）

## 一、复核方结论要点

- **红线核验**：① ② ④ ⑤ ⑥ ⑨ 不适用（本批未触碰播放调度/音频回调/进程隔离/NativeGate/PCM 分块/SAF 导入）；③ ⑦ ⑧ 适用且合规—— ③「不假成功」以**真实 200 + JSON 解析 + 真实测试执行**为证；⑧ 指出 `manifest.json` 仅改 `note` 文案，`schema`/`version`/SHA-256 元数据未动，不影响哨兵校验。
- **改名风险**：`rootProject.name` 变更只影响根构建缓存键（一次性重建），不改变 AAR/APK 产物路径与 CI 依赖。
- **清单 URL 404**：确认「GitHub 重命名后 raw 与加速镜像**不提供稳定重定向**」的机制判断，与实施线实测一致；旧客户端降级路径＝缓存 → 内置兜底，**不崩溃**。
- **README 事实核验**：逐条复核 ABI / 三件套版本 / 无条件告知 / 显示名说明四处，**未发现残留过时事实**。
- **文档纪律**：三件套升版与变更记录合规；确认「头部版本行同步」缺陷已被本次修正（对应错题本 C9 类问题）。

## 二、实施线处置

| # | 复核意见 | 处置 |
|---|---|---|
| 1 | 结论：**通过** | **采纳**（本批可直接提交，待甲方许可） |
| 2 | **非阻塞运维建议**：重建/保留 `kermond369-nsis/ebook2tts-local` 占位仓（仅放 `manifest.json`），使 v1.0.1 存量旧客户端的旧 RAW_URL 恢复 200 | **待甲方裁定** —— 涉及 GitHub 写操作（建仓），按纪律须逐次授权；另注：仓库重命名后旧名会被占位重定向，**同名占位仓可能无法创建**，届时需接受「旧客户端降级到内置清单」或改用其它兼容手段 |
| 3 | 行号引用核验 | 实施线**逐条回查**全部引用（README L60/73/125/127、settings L15、manifest L5、三件套 L3/107/219/278/27/533、ManifestRepository L91）——**本次无编造行号**；仅 README:150 为表头分隔行（实质引用为紧邻的告警行，方向正确） |

## 三、验证证据（本批）

| 项 | 命令 | 结果 |
|---|---|---|
| Kotlin 单测 | `./gradlew :core:test :engine:testDebugUnitTest` | **BUILD SUCCESSFUL** |
| Flutter 测试 | `flutter test` | **3/3 全绿** |
| 静态分析 | `flutter analyze` | 零告警 |
| 清单 URL | 旧址 / 新址 / ghfast.top / ghproxy.net | **404 / 200 / 200 / 200**，JSON 解析通过（version=2026091301，3 模型） |

**原始复核输出**：交付目录 `命名统一批次-20260921/附件X-agy原始复核输出.txt`。

## 四、状态

本批改动**已实施、已自测、已复核通过**；**未提交、未 push**（等甲方许可）。
